import React from 'react'
import PropTypes from 'prop-types'

const AppFrame = props => {
    return (
        <div>
            AppFrame
        </div>
    )
}

AppFrame.propTypes = {

}

export default AppFrame
